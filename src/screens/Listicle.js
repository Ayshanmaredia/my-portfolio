import React from 'react';
import ProjectDetails from '../components/ProjectDetails';

const Listicle = ({ projects }) => {
    return (
        <ProjectDetails projects={projects} />
    )
};

export default Listicle;
